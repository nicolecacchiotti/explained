[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SITE_PASSWORD"

# Note: Redirects are handled by public/_redirects file
# The _redirects file uses 301! (force) to override cached Cloudflare redirects
